# Anomaly audit — bioenergy slice (`aeo9_v0.67_w_results`)

This is **your team's slice of the full canon anomaly audit** (`CANON_ANOMALY_AUDIT_20260704.md`, generated 2026-07-04 by an offline sweep over the four scenarios that matter — Current Accounts, Baseline Simulation, AMS Target Scenario, Regional Aspiration Scenario). It carries over, verbatim and unchanged, only the findings that touch branches **bioenergy owns or authors**: the supply fuels under `Resources\Primary\` and `Resources\Secondary\` (the five CSV crops — Cassava, Coconut Oil, Corn, Palm Oil, Sugarcane — plus Molasses, Palm Oil Mill Effluent, Bagasse, Wood, Biomass, and the Arable/Perennial land pseudo-fuels) and the two Key drivers wired to your sector (`Key\Optimized Trade`, `Key\Biofuel Blending Targets`). We have **not** changed any counts, tags, or grades, and we have **not** added new findings. Each item keeps its **NEW/KNOWN** and **VERIFIED/SUSPICIOUS** flags; Part-B items keep their 🔴/🟡/🟢 grade. Coal/oil/gas/NGL/nuclear/refined-product items are the fossil team's and renewable-power caps are the power team's — those were deliberately left out (see the exclusion note at the end). You are the right people to **judge and fix** the items below; where something is clearly deliberate design (Arable/Perennial), we flag it as such. "Cross-tree note" lines mark defects that live in a Key/Resources branch your sector merely references, so you know they are upstream of you and owned elsewhere.

---

## Part A — Incorrectly inputted (anomalies in authored values)

### A7. Cross-region outliers / unit slips

- **NEW · SUSPICIOUS — Brunei Biomass Maximum Production = 8,773 TWh**, ~1,600× the sibling median and larger than all-ASEAN primary energy — near-certain unit slip (GWh/TJ intended). **Propagates**: Brunei Bagasse & Wood caps are authored as `Biomass:Maximum Production[TWh]`. *12 rows. Resources.*

### A12. Comment hygiene hiding data problems

- **KNOWN — resources `?~Former expression:` reverted caps** (RAS less constrained than ATS on **Thailand Biomass**), `~`-dialect ×101, `_x000D_` in comments ×32. *Resources.*
  > Bioenergy-relevant slice: the reverted-cap case explicitly named here is **Thailand Biomass**, one of your Primary fuels — RAS ends up *less* constrained than ATS. The `~`-dialect (×101) and `_x000D_`-in-comment (×32) counts are the whole Resources tree, not bioenergy-only.

---

## Part B — Empty but important (graded)

### 🔴 RED — breaks the calc or actively distorts LP/results in the 4 scenarios now

2. **Zero-cost open supply/import routes.** *(KNOWN #24 · Resources)* In RAS, **191 (fuel,region) pairs have Maximum Production ≠ 0 with Production Cost = 0** (incl. Nuclear at Unlimited + $0), and **95 pairs have open Maximum Imports with Import Cost = 0** (Refinery Feedstocks/Gas, Renewable Diesel, Arable/Perennial ×12). **Mechanism:** a cap-open, cost-zero route is a free lunch the LP exploits regardless of realism — the exact mechanism behind the 2026-05-18 biodiesel-to-Timor-Leste and 2026-05-19 POME incidents.
   > This RED class spans several sectors' fuels (Nuclear and Refinery Feedstocks/Gas belong to fossil/power). The bioenergy-owned members are the ones to judge/fix on your side: the **Arable/Perennial ×12** open-import pairs, plus the open-production biofuels/crops that carry this shape (per your handover README §7.1: Secondary Biodiesel, Ethanol, Domestic Biogas, Renewable Diesel, Sustainable Aviation Fuel across all 12 regions; Corn and POME on Base Template + Timor Leste; Cassava among the `0.001`-placeholder import prices). The named incident history (biodiesel-to-Timor-Leste, POME) is on your fuels — this is the single most consequential class for bioenergy.

### 🟡 YELLOW — placeholder/template values silently shaping results (wrong numbers, not broken mechanics)

**Resources**

- **Consumer prices ~95 % zero** on the branches demand regressions reference (1,130 of 1,188 cells; **Bagasse**/coals/NG/MSW Industrial price 44/44 zero). **Mechanism:** the Exp/Ln price-elasticity shells evaluate `Ln(0)` → undefined or garbage, so fuel-switching response is silently priced at zero. (Same class hits industry's referenced prices.)
  > Bioenergy-owned member of this class: **Bagasse** Industrial Consumer Price (44/44 zero). The coals/NG/MSW members of the same 44/44 count are fossil/power/industry, not yours.

- **`Unlimited ? tbc` placeholder caps** survive on **Biomass**/Geothermal/Large Hydro/MSW (37 rows) → un-capped renewable supply in the very RAS scenario whose RE targets those caps should bind.
  > Bioenergy-owned member: **Biomass** (a Primary fuel you own). Geothermal / Large Hydro / MSW in the same 37-row group are power's renewable-generation caps — see the cross-tree note below.

- **NEW — Production Cost = `0.001` template** on the 7 variable renewables + Geothermal-class (all scenarios) and the **crops/Molasses**/MSW (CA/Baseline/ATS only — RAS has real injected costs). For crops this means the three non-optimized scenarios value feedstock at ~$0 → cross-scenario biofuel cost results are not comparable.
  > Bioenergy-owned members: the **crops and Molasses** carrying `Production Cost = 0.001` in Current Accounts / Baseline / AMS Target (RAS already holds your real injected costs). The 7 variable renewables + Geothermal-class in the same bullet are power's; MSW is not yours.

- **Arable/Perennial land pseudo-fuels** carry Maximum Imports = Unlimited at Import Cost 0 (RAS), and Perennial's cap is mis-tagged "Cubic Meter" vs Arable's "Thousand GJ" — free unlimited "land imports" if a trade route is ever enabled; the unit drift also breaks the GJ/ha anchor.
  > Fully bioenergy-owned. Note this is the *land-as-fuel* design (1 GJ ≈ 1 ha anchor) — the anchor itself is intentional; the flagged defects are the **Import Cost = 0 open route** and the **Perennial "Cubic Meter" unit-tag drift**, not the double-cap design.

### 🟢 GREEN — cosmetic, disabled plumbing, or plausibly-intentional zeros

- **Resources:** all-zero series on *closed* routes (cost 0 paired with cap 0 — unreachable today, but a tripwire target: reopening a cap without its cost row recreates the RED #2 exploit); "U.S. Dollar" vintage-less units (mostly on zero cells).
  > Applies to your closed-route fuel/region cells too: harmless *while* the cap is 0, but any future cap-reopen without a paired cost row re-arms the 🔴 B2 free-lunch class. Treat as a standing tripwire whenever you open a new production/import route.

---

## Cross-tree notes (defects upstream of you, owned elsewhere)

- **🔴 B2 — non-bioenergy members of the zero-cost-route class.** Nuclear (Unlimited + $0), Refinery Feedstocks / Gas, and the other non-feedstock fuels inside the 191 production / 95 import pairs are **fossil/power**. Your slice is only the Arable/Perennial ×12 imports and your open-route biofuels/crops (above).
- **🟡 `Unlimited ? tbc` renewable caps — Geothermal / Large Hydro / MSW** (part of the 37-row group) are **power**'s renewable-generation caps. Only Biomass in that group is yours.
- **🟡 Production Cost = 0.001 — the 7 variable renewables + Geothermal-class** portion of that bullet is **power**'s; only the crops/Molasses portion is yours.
- **🟡 Consumer-price `Ln(0)` class — coals / Natural Gas** members are **fossil**; MSW Industrial is **power/industry**. Only Bagasse is yours.
- **`Key\Optimized Trade` (495 routes) and `Key\Biofuel Blending Targets`** are the Key drivers your sector wires into. The master audit does not raise a *standalone* defect on these Key branches, but they are the enabling mechanism for the 🔴 B2 exploit on your feedstocks: a blend mandate with an open, zero-cost import route lets the LP source the whole region's feedstock through the free route. When you fix a zero-cost route on a traded feedstock (Biodiesel, Cassava, Coconut Oil, Corn, Ethanol, Molasses, Palm Oil, POME, Sugarcane), check the matching trade-route switch state (on in RAS/CNZ only). This is your tree, flagged here for completeness — no new finding is being asserted.

---

## Highest-leverage fixes for your team

1. **Brunei Biomass 8,773 TWh unit slip (A7).** Near-certain GWh/TJ→TWh slip; it propagates into Brunei Bagasse and Wood, which are authored *as* `Biomass:Maximum Production[TWh]`. One correction cascades to three of your fuels. *12 rows, SUSPICIOUS — confirm the intended unit/magnitude.*
2. **Zero-cost open routes on your feedstocks (🔴 B2).** The documented free-lunch class with real incident history on your fuels (biodiesel-to-Timor-Leste, POME). Confirm each open-production biofuel/crop either has its cost on the production process (fine) or needs a Production Cost / Import Cost trajectory; replace the Cassava `0.001` import-cost placeholder.
3. **Arable/Perennial open "land imports" + Perennial unit-tag drift (🟡).** Close the Import Cost = 0 open route and fix Perennial's "Cubic Meter" tag back onto the Thousand-GJ / GJ-per-ha anchor — without touching the intentional double-cap design.
4. **Crop / Molasses `Production Cost = 0.001` template in CA / Baseline / ATS (🟡).** RAS already holds your real injected costs; bring the three accounting scenarios into line so cross-scenario biofuel cost comparisons are valid.
5. **Thailand Biomass reverted cap (A12) and Bagasse zero Industrial Consumer Price (🟡).** Confirm RAS should not be *less* constrained than ATS on Thailand Biomass, and price Bagasse so its elasticity shell doesn't evaluate `Ln(0)`.

---

## Transformation anomalies

This section is the **Transformation slice** of the bioenergy anomaly audit — the conversion processes under `Transformation\` that your feedstocks feed (Biodiesel / Bioethanol / Renewable Diesel / SAF / Biomethane / Domestic Biogas / Charcoal + Hydrogen / Methanol / Ammonia Production, 325 branches across 10 module groups). It comes from the same 2026-07-04 offline sweep over the four scenarios that matter (Current Accounts, Baseline Simulation, AMS Target Scenario, Regional Aspiration Scenario), **verifier-checked** — every finding below carries a CONFIRMED verdict with the count reproduced exactly. Canon reference is anatomy [§14](../../../LEAP%20structure/LEAP_STRUCTURE_ANATOMY.md) + hygiene-ledger rows #33/#34/#35.

**Headline: no 🔴 in the bioenergy Transformation slice.** The RED classes in the Transformation export (free unlimited power capacity; `Exogenous Capacity = Unlimited` forced floors; the must-run trap) are all **owned by power and fossil**, not bioenergy — verified 0 bioenergy rows on those shapes (see *Cross-tree notes* + *Verified-clean* below). The graded bioenergy items are **4 🟡 + 2 🟢**. Part A below adds the incorrectly-inputted class (NEW/KNOWN + VERIFIED/SUSPICIOUS, not colour-graded, matching this document's Part-A convention).

### Part A — Incorrectly inputted (Transformation processes)

- **A-T1. NEW · VERIFIED — solver-writeback / input-output conflation: `Optimized New Capacity` holds solved `Data(...) ?Optimized on … (NEMO/CPLEX)`.** `288 rows` (24 bioenergy process branches × 12 regions), **Regional Aspiration Scenario only** (0 in CA / Baseline / AMS Target); 23 of the 288 carry a non-zero `Data(...)` series. Example: `Transformation\Sustainable Aviation Fuel Production\Processes\HVO Renewable Diesel | Indonesia | RAS = Data(2030, 11424.48, 2040, 67293.74, 2050, 198133.2, 2060, 244866.9) ?Optimized on 07/02/2026 11:41 (NEMO/CPLEX)`. The RAS export is a **post-solve snapshot**, not a clean input area — any consumer treating it as authored assumptions ingests CPLEX solution capacity. *Verifier:* the identical writeback also lives byte-for-byte in Carbon Neutrality/Net-Zero (outside the 4-scenario scope). This phenomenon **is** documented in canon §14 §1.1 (power side) and §14 §4 as the §14-ledger artefact — here on the supply side — but is not a numbered bio-specific ledger row. *(This is the cross-domain solver-writeback fact — same shape as the 426-row Optimized New Capacity `Data()` conflation seen area-wide.)*

- **A-T2. NEW · VERIFIED (latent) — sibling-variant inconsistency: `HVO Renewable Diesel` twin has `Capital Cost = 0` + `Variable OM Cost = 0` under Renewable Diesel Production.** `88 rows`. The byte-identical process is authored with **full** cost under `Sustainable Aviation Fuel Production` but with `Capital Cost = 0` (44 rows = 11 regions × 4 scenarios, all except Indonesia) and `Variable OM Cost = 0` (44 rows) under `Transformation\Renewable Diesel Production\Processes\HVO Renewable Diesel`. HVO RD is capacity-planned (full 29-var panel) and builds only from optimization, so a $0 capital cost makes new Renewable Diesel capacity free to the LP in 11 regions — **including real ones** (Malaysia / Thailand / Vietnam / Philippines). **Latent:** RD-module `Optimized New Capacity = 0` in every region in the current RAS solution, so not yet exploited, but the cost landscape is distorted. *Verifier:* the SAF twin carries the real cost via `RegionValue(Indonesia)` outside Indonesia; net "SAF twin has authored cost in all 12 regions" holds.

- **A-T3. NEW · VERIFIED — placeholder confession: Cassava & Molasses Bioethanol `Capital Cost` + `Variable OM Cost` = a ref to Sugarcane tagged "Placeholder pending data".** `152 rows` = Cassava{Capital 38, VOM 38} + Molasses{Capital 38, VOM 38}; by scenario Current Accounts 48 / Baseline 48 / AMS Target 48 / RAS 8. Verbatim: `Sugarcane:Variable OM Cost[2020 USD/GJ] ? Placeholder pending data for this process` (and the Capital Cost analogue). *Verifier:* in RAS the placeholder survives **only** on Base Template + Timor Leste (both disabled); CA / Baseline / AMS are accounting (LEAP-simulation) scenarios with no cost-minimizing LP, so the live LP-economic exposure across the 4 scopes is minimal — valid as a data-hygiene placeholder-confession regardless.

- **A-T4. NEW · SUSPICIOUS (needs human judgment) — biogenic-vs-fossil CO2: `Biomass Gasification with CCS` zeroes gross feedstock CO2 on both leaves + books a flat `-203882` Sequestered CO2 (including the fossil Natural Gas leaf).** `192 rows`. `Carbon Dioxide = 0 * …\Biomass Gasification with CCS:Process Efficiency/100` on both the `Biomass` and the (fossil) `Natural Gas` feedstock leaves (96 rows); `Sequestered Carbon Dioxide = -203882 * …:Process Efficiency/100` on the same 2 leaves (96 rows). That credit is **66.2×** the non-CCS twin's gross biomass CO2 (`3079.624`) and is authored inconsistently with the fossil-CCS siblings, which **keep** their gross and subtract 95% (SMR-CCS seq `-82467 * 95% *…`; Coal-Gas-CCS seq `-151157.9 * 95% *…`). Applying `-203882` to the **fossil** Natural Gas feedstock turns fossil gas use into apparent net carbon removal. *Verifier:* could be intentional BECCS net-negative accounting, so tagged SUSPICIOUS not a hard defect — but the flat `-203882` landing on the fossil NG leaf + the 66× magnitude are the legitimate human-review red flags.

- **A-T5. NEW · VERIFIED (cross-tree) — zero-cost open feedstock route (POME-lesson shape): Corn Ethanol feedstock is free AND unlimited; the `0.001` arbitrary-production guard is missing.** `72 rows`. `Resources\Primary\Corn:Production Cost = 0` and `Maximum Production = Unlimited` in Current Accounts, Baseline, and AMS Target (12 regions each → 36 + 36). Every sibling ethanol crop (Cassava, Molasses, Sugarcane) plus Palm Oil / Coconut Oil carries the guard `0.001 ? Very small cost to avoid arbitrary production in optimization`; **Corn alone is left at literal 0.** This is the requested feedstock Fuel-Cost → zero-Resources-price trace: Corn Ethanol Feedstock `Fuel Cost = 0` defers to a Corn `Production Cost` that is also 0. RAS fixes it for the 10 real regions (real Interp cost + capped Maximum Production); the +2 RAS stub rows are Base Template + Timor Leste. *Verifier:* the general class is canon ledger #24 (191 MaxProd≠0 & ProdCost=0 pairs) but Corn/guard-omission is not named there; the 3 free+unlimited scenarios are accounting runs (no LP) and RAS fixes it, so live LP exploitation is minimal — a genuine data-hygiene inconsistency with low LP impact now.

- **A-T6. NEW · SUSPICIOUS — emission-factor gap: `Methane = 0` on three crop feedstock leaves that carry an authored CO2 factor.** `144 rows` = Coconut Oil (CME Biodiesel) 48 + Cassava (Bioethanol) 48 + Sugarcane (Bioethanol) 48 (12 regions × 4 scenarios each). `Avg Environmental Loading = 0` on the `…\Methane` leaf while the same-branch `Carbon Dioxide` is authored (e.g. Cassava CO2 = `24.95 ? bioenergydat`). *Verifier:* these are the **only** Methane=0 leaves in bioenergy in-scope (every other Methane leaf is non-zero, 1,296 rows) — CH4 from feedstock handling is dropped for these three crops but kept elsewhere. Plausibly a deliberate minor-agricultural-CH4 omission, but inconsistent across crops; SUSPICIOUS, not a hard defect.

- **A-T7. KNOWN (canon) · VERIFIED — `Unlimited` literal → 1e12 upper-bound sentinel on `Maximum Production`, all 24 bioenergy processes in RAS.** `288 rows` (24 branches × 12 regions), **RAS only** (the variable materializes only in the optimization bloc). Per §A.11 the literal exports as the `1.0e+12` sentinel that pollutes LP conditioning; for the **7 lite-panel processes** (Charcoal, Domestic Biogas, both Biomethane AD, both Methanol, Ammonia) it is the ONLY quantity cap, so those routes are effectively uncapped. *Verifier:* documented in canon §14 §2 variable inventory ("Maximum Production 9,072/108; all Unlimited") + ledger #34 — **not** ledger #8 (that's the Residential AC/Refrigeration solver-writeback). This is the **upper-bound** 1e12 sentinel (conditioning pollution / uncapping the lite-panel routes), *not* the RED lower-bound forced floor.

- **A-T8. KNOWN (canon) · VERIFIED — `Unlimited` literal → 1e12 capacity-bound sentinel on `Maximum Capacity` + `Maximum Capacity Addition` in RAS.** `328 rows` = `Maximum Capacity = Unlimited` 124 rows (14 branches — Hydrogen ×8 + Pressurized H2 + HVO RD + SAF and peers) + `Maximum Capacity Addition = Unlimited` 204 rows (17 branches), both RAS. Same upper-bound sentinel on the capacity bounds, leaving new Hydrogen / Renewable Diesel / SAF capacity uncapped in the optimized scenario. *Verifier:* defensible KNOWN (ledger #34 fossil-blending analog, ledger #23 Resources); upper-bound sentinel, not the RED floor.

### Part B — Empty but important (graded: 4 🟡 + 2 🟢)

#### 🟡 YELLOW — placeholder/template zeros silently shaping results

- **B-T1. NEW — conversion step is FREE and UNCAPPED on 5 lite-panel biofuel processes.** `5 processes`: Charcoal `All Biomass` (VOM = 0), Domestic Biogas `Anaerobic Digestion` (VOM = 0), Methanol `CO2 Utilization for Iron and Steel` (no VOM variable), Methanol `Production from Hydrogen` (no VOM variable), Ammonia `Hydrogen` (no VOM variable) — all with **no Capital Cost variable at all** and `Maximum Production = Unlimited` in RAS. **Mechanism:** zero capex + zero process VOM + an Unlimited (1e12) production ceiling means the only brake is feedstock cost/supply; in an optimized run the LP can convert feedstock to Charcoal / Biogas / Methanol / Ammonia at zero process cost, distorting the merit order toward these routes. *Verifier:* correctly **excludes** the 2 Biomethane AD variants (`Anaerobic Digestion with Methanation` / `with Upgrading`) — they also lack Capital Cost and are uncapped, but carry a **non-zero** Variable OM Cost (48 rows each), so they are not "free".

- **B-T2. NEW — `Variable OM Cost = 0` on the 6 capacity-planned Hydrogen plants.** `288 rows`: SMR, SMR with CCS, Coal Gasification, Coal Gasification with CCS, Biomass Gasification, Biomass Gasification with CCS — VOM = 0 (48 rows each = 12 regions × 4 scenarios); all 288 companion `Capital Cost` rows are authored non-zero. Zero variable O&M understates the marginal cost of hydrogen from these routes, biasing NEMO's dispatch and build toward them vs routes that do carry VOM (e.g. PEM Electrolysis pays for electricity). *Verifier:* the mechanism is **LIVE, not latent** — in RAS these routes are actually built (SMR gets non-zero Optimized New Capacity in 4 regions, Biomass Gasification in 2, PEM Electrolysis in 3), so the zero VOM does understate an exercised route; yellow is well-founded (arguably borderline-stronger for SMR / Biomass Gasification specifically).

- **B-T3. NEW — `Fixed OM Cost = 0` across every liquid-biofuel plant.** `432 rows` = 9 processes × 48 (CME / FAME / POME Biodiesel, Cassava / Corn / Molasses / Sugarcane Ethanol, HVO Renewable Diesel [RD 48 + SAF 48]) = 432 of 816 process-level bio Fixed OM Cost rows are literal 0, in all regions and scenarios. No fixed O&M systematically understates their annual carrying cost, making them look cheaper to keep operating than reality and shifting the cost comparison against fossil / other routes.

- **B-T4. KNOWN-class (canon ledger #24 / §14 §1.4) — biofuel feedstock is effectively free: 1,337 of 1,824 feedstock `Fuel Cost` rows = 0.** `1,337 rows`. Cost is deferred to Resources at the Transformation stage, and the Resources crops they defer to carry only the near-zero `0.001 ? Very small cost to avoid arbitrary production in optimization` (Palm Oil, Coconut Oil, Cassava, Molasses, Sugarcane) — with **Corn left at literal 0** (see A-T5). **Mechanism:** the only feedstock brake the LP sees is the crop supply cap, not price — the exact POME-lesson exposure (every supply cap needs a real companion cost or the LP routes through the near-zero-cost region); where the cap is also Unlimited (Corn) there is no brake at all. *Verifier:* 1,337 / 1,824 in-scope reproduced exactly (all-scenario figure 3,715 / 5,016 matches canon §14 §1.4); caveat — in RAS the deferred Resources Production Cost **is** real (Interp trajectories) for the 10 real regions, so the near-free values dominate the accounting scenarios (no LP) rather than RAS. The finding's "0.001 … 37 rows each" sub-detail did not reproduce (the guard appears in 110/132 all-scenario rows, 101 for Sugarcane); the core count + POME-lesson pattern stand.

#### 🟢 GREEN — cosmetic or plausibly-intentional zeros

- **B-T5. NEW — template-uniform Capital Cost: FAME Biodiesel (and peer liquid-biofuel plants) carry one identical trajectory across all 10 real AMS.** `10 regions`. `FAME Capital Cost = Interp(2025, 3.2422, 2030, 3.0833, 2035, 2.9322, 2040, 2.7885, 2045, 2.6518, 2050, 2.5219, 2055, 2.3983, 2060, 2.2807)` identical for Brunei..Vietnam in RAS; Base Template & Timor Leste = 0 (benign placeholder/disabled). Masks real regional capex differences; may be an intentional single assumption, but the export carries no per-country capex signal for these plants.

- **B-T6. GREEN — zeroed accounting block: Salvage Value, Stranded Cost, Module Costs and Output Price are literal 0 on 100% of bioenergy rows.** `2,400 rows` = Salvage Value 0/816 nonzero + Stranded Cost 0/816 + Module Costs 0/384 + Output Price 0/384. End-of-life salvage and stranded-asset costs are absent from every biofuel / H2 plant — cosmetic for a pure levelised-cost read, but it removes those terms from any total-system-cost or asset-stranding analysis built on this export.

#### Verifier addendum + verified-clean (not counted in the 4🟡/2🟢)

- **🟢 NEW · VERIFIED (addendum, missed by the original sweep) — Excel carriage-return artifacts `_x000D_` inside `Process Efficiency` expressions.** `143 bioenergy rows`, all on `Process Efficiency` — Anaerobic Digestion 132 + FAME Biodiesel 11; 52 fall in the 4 in-scope scenarios. Green/cosmetic, but a real hygiene defect the findings missed: canon ledger #14 records `_x000D_` only for Commercial / Residential / Transport — **not** Transformation.
- **Cleared during verification — no fabricated RED for bioenergy.** The 88 bioenergy rows with `;` inside an `Interp()` expression are **not** §A.15 separator violations — every semicolon sits in the trailing `?` citation comment (e.g. `…Data in ASEAN.xlsx; production capacity distributed…`), 0 inside any Interp arg list. And **no bioenergy process carries `Exogenous Capacity = Unlimited` (0 rows) or a bare `Minimum Utilization = Maximum Availability` (0 rows)** in the 4 scopes — so the two RED traps that hit fossil (§A.11) and power (§11.2c) do **not** touch bioenergy Transformation.

### Cross-tree notes (Transformation defects owned elsewhere)

These are the RED / SUSPICIOUS Transformation findings from the master hunt that live on **power** or **fossil** branches, flagged here for completeness so you know they are upstream of you and owned elsewhere — no new bioenergy finding is asserted.

- **🔴 Power (canon ledger #35 + §14 §1.1).** Six Malaysia `_MY*` generators (Solar PV_MY*, Gas Turbine_MYPE, Large Hydro_MYPE, Wind Onshore_MYSR …) carry `Capital Cost = 0` **and** `Fixed / Variable OM Cost = 0`, combined with `Maximum Capacity Addition = Unlimited` (70 rows) → **free unlimited capacity the LP exploits**. Owned by power.
- **🔴 Fossil (canon ledger #34).** The 4 biofuel-mandate **blending pseudo-techs** (Gasoline / Diesel Blending) carry `Exogenous Capacity = 'Unlimited'` in **Megawatt** → the §A.11 `1e12` **forced FLOOR** (the 2026-05-12 p9 shape). Owned by fossil. *(Distinct from bioenergy's upper-bound `Unlimited` sentinels A-T7/A-T8, which are caps not floors.)*
- **🟡-with-caveat Power (canon ledger #33).** The §11.2c must-run trap — a bare `Minimum Utilization = Maximum Availability` on variable renewables, **28 rows in RAS** — is **AUTHORED but VERIFIED INERT**: every trap branch has `Exogenous Capacity = 0` / Node = 0 / `Optimized New Capacity = 0`, so it binds nothing. Authored hazard, currently inert because zero capacity; would bite if those `_MY*` branches ever get capacity. Owned by power.
- **SUSPICIOUS Power/fossil.** `ScenarioValue(Bad Scenario [2])` dangling reference inside `Endogenous Capacity`, **20 rows in AMS Target** — RED if LEAP errors on the missing scenario, YELLOW if it evaluates to 0. Marked SUSPICIOUS pending a LEAP-eval check. Owned elsewhere.
